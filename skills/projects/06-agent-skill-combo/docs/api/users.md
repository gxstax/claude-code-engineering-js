# Users API

> Auto-generated by api-generating Skill. Do not edit manually.

## Base Path

`/api/users`

---

## Endpoints

### GET `/`

🔒 Requires authentication (requireAuth + isAdmin)

**Description**: List all users (admin only). Supports pagination via query parameters.

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| page | query | integer | false | Page number for pagination (default: 1) |
| limit | query | integer | false | Number of items per page (default: 20) |

**Response**:

| Status | Description |
|--------|-------------|
| 200 | A paginated list of users |

**Response Example**:

```json
{
  "data": [
    {
      "id": "1",
      "name": "John Doe",
      "email": "john@example.com"
    }
  ],
  "page": 1,
  "limit": 20
}
```

---

### GET `/:id`

🔒 Requires authentication (requireAuth)

**Description**: Get a single user by their unique ID.

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | true | The unique identifier of the user |

**Response**:

| Status | Description |
|--------|-------------|
| 200 | The requested user object |
| 404 | User not found |

**Response Example**:

```json
{
  "data": {
    "id": "1",
    "name": "John Doe",
    "email": "john@example.com",
    "createdAt": "2025-01-01T00:00:00Z"
  }
}
```

---

### POST `/`

**Description**: Create a new user. No authentication required (registration endpoint).

**Parameters**: None

**Request Body**:

```json
{
  "name": "John Doe",
  "email": "john@example.com",
  "password": "securePassword123"
}
```

**Response**:

| Status | Description |
|--------|-------------|
| 201 | The newly created user object |
| 400 | Invalid input (validation error) |

**Response Example**:

```json
{
  "data": {
    "id": "2",
    "name": "John Doe",
    "email": "john@example.com",
    "createdAt": "2025-06-22T12:00:00Z"
  }
}
```

---

### PUT `/:id`

🔒 Requires authentication (requireAuth)

**Description**: Update an existing user's information. Uses `updateUserSchema` for validation.

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | true | The unique identifier of the user to update |

**Request Body**:

```json
{
  "name": "Jane Doe",
  "email": "jane@example.com"
}
```

**Response**:

| Status | Description |
|--------|-------------|
| 200 | The updated user object |
| 400 | Invalid input (validation error) |
| 404 | User not found |

**Response Example**:

```json
{
  "data": {
    "id": "1",
    "name": "Jane Doe",
    "email": "jane@example.com",
    "updatedAt": "2025-06-22T12:00:00Z"
  }
}
```

---

### DELETE `/:id`

🔒 Requires authentication (requireAuth + isAdmin)

**Description**: Delete a user by their unique ID. Admin-only operation.

**Parameters**:

| Name | In | Type | Required | Description |
|------|----|------|----------|-------------|
| id | path | string | true | The unique identifier of the user to delete |

**Response**:

| Status | Description |
|--------|-------------|
| 204 | User successfully deleted (no content) |
| 401 | Missing or invalid auth token |
| 403 | Insufficient permissions (non-admin) |

**Response Example**: No content.

---

## Error Codes

| Code | Message | Description |
|------|---------|-------------|
| 400 | Bad Request | Invalid input parameters |
| 401 | Unauthorized | Missing or invalid auth token |
| 403 | Forbidden | Insufficient permissions |
| 404 | Not Found | Resource does not exist |
| 500 | Internal Server Error | Unexpected server error |
